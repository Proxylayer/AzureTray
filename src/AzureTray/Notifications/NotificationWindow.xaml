<Window x:Class="AzureTray.Notifications.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notification"
        Width="360"
        SizeToContent="Height"
        MinHeight="140"
        MaxHeight="360"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="Manual"
        Background="Transparent"
        AllowsTransparency="True"
        Foreground="{DynamicResource Brush.Text.Primary}"
        FontFamily="{DynamicResource Font.Family.UI}"
        FontSize="{DynamicResource Font.Size.Body}"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Border Background="{DynamicResource Brush.Surface.Panel}"
            BorderBrush="{DynamicResource Brush.Border.Default}"
            BorderThickness="1"
            CornerRadius="{DynamicResource Radius.Large}"
            Padding="14"
            Margin="12">
        <Border.Effect>
            <DropShadowEffect Color="Black"
                              Opacity="0.55"
                              BlurRadius="16"
                              ShadowDepth="2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0">
                <Button DockPanel.Dock="Right"
                        Content="×"
                        FontSize="16"
                        Width="22" Height="22"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{DynamicResource Brush.Text.Muted}"
                        Cursor="Hand"
                        Command="{Binding DismissCommand}"
                        ToolTip="Dismiss"/>
                <TextBlock Text="{Binding Title}"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Brush.Text.Primary}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
            </DockPanel>

            <TextBlock Grid.Row="1"
                       Text="{Binding Message}"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource Brush.Text.Secondary}"
                       Margin="0,8,0,8"/>

            <!-- Choice input -->
            <StackPanel Grid.Row="2"
                        Visibility="{Binding IsChoice, Converter={StaticResource BoolToVis}}">
                <ListBox ItemsSource="{Binding Choices}"
                         SelectedItem="{Binding SelectedChoice, Mode=TwoWay}"
                         BorderBrush="{DynamicResource Brush.Border.Default}"
                         BorderThickness="1"
                         Background="{DynamicResource Brush.Surface.Input}"
                         Foreground="{DynamicResource Brush.Text.Primary}"
                         MaxHeight="120"/>
                <StackPanel Margin="0,8,0,0"
                            Visibility="{Binding AllowOther, Converter={StaticResource BoolToVis}}">
                    <RadioButton Content="Other:"
                                 IsChecked="{Binding IsOtherSelected, Mode=TwoWay}"
                                 Foreground="{DynamicResource Brush.Text.Primary}"
                                 GroupName="NotificationOther"/>
                    <TextBox Text="{Binding OtherText, UpdateSourceTrigger=PropertyChanged}"
                             IsEnabled="{Binding IsOtherSelected}"
                             Margin="20,4,0,0"
                             Padding="4,2"/>
                </StackPanel>
            </StackPanel>

            <!-- TextInput input -->
            <TextBox Grid.Row="2"
                     Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                     Padding="6,4"
                     Visibility="{Binding IsTextInput, Converter={StaticResource BoolToVis}}"/>

            <!-- Action buttons -->
            <Grid Grid.Row="3" Margin="0,12,0,0">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Visibility="{Binding IsYesNo, Converter={StaticResource BoolToVis}}">
                    <Button Content="Yes"
                            Width="80"
                            Padding="0,4"
                            Margin="0,0,8,0"
                            Command="{Binding YesCommand}"/>
                    <Button Content="No"
                            Width="80"
                            Padding="0,4"
                            Command="{Binding NoCommand}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Visibility="{Binding IsSubmittable, Converter={StaticResource BoolToVis}}">
                    <Button Content="Submit"
                            Width="80"
                            Padding="0,4"
                            Margin="0,0,8,0"
                            Command="{Binding SubmitCommand}"/>
                    <Button Content="Cancel"
                            Width="80"
                            Padding="0,4"
                            Command="{Binding DismissCommand}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
